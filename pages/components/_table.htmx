<div id="instructions">
</div>

<pagedtable list="list" total="total" page="page" id="table">
	<column title="Code">${prepareCode(item)}</column>
	<column title="Name">${item.name || ""}</column>
</pagedtable>


<div id="val">${document.getElementById('table').innerHTML.replace(/\s/g, '')}</div>